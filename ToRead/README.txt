Make sure you have pygame installed.

Pythons current version does not like pygame for some reason so you have to install the community edition.
With python installed run this in your terminal: py -m pip install pygame-ce.
